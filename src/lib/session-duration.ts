/** Default session lifetime when "Remember me" is unchecked (8 hours). */
export const SESSION_MAX_AGE_DEFAULT = 60 * 60 * 8

/** Extended session lifetime when "Remember me" is checked (30 days). */
export const SESSION_MAX_AGE_REMEMBER = 60 * 60 * 24 * 30

export const REMEMBER_EMAIL_STORAGE_KEY = 'vrsps-remember-email'

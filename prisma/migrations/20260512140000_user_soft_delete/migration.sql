-- Placeholder: restores missing migration file (fixes Prisma P3015 when this name
-- exists in `_prisma_migrations` or the chain expects this folder on disk).
-- Current `User` model has no soft-delete columns; this is a no-op for new DBs.
SELECT 1;
